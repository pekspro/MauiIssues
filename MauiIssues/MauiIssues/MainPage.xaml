<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:v="clr-namespace:MauiIssues"
             x:Class="MauiIssues.MainPage"
             Title="{Binding SpanCount}"
             >
    <Grid RowDefinitions="*, auto">
        <CollectionView ItemsSource="{Binding Numbers}">
            <CollectionView.ItemsLayout>
                <GridItemsLayout 
                    Orientation="Vertical"
                    Span="{Binding SpanCount}"
                    VerticalItemSpacing="4"
                    HorizontalItemSpacing="4" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid HeightRequest="100" WidthRequest="100" BackgroundColor="DarkBlue">
                        <Label  Text="{Binding .}"
                                TextColor="LightCyan"
                                HorizontalOptions="Center"
                                VerticalOptions="Center"
                                />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Grid.Row="1" Text="Add Column" Clicked="AddColumn_Clicked" />
    </Grid>
</ContentPage>
